## Otomatik Deploy
Her değişiklik tamamlandıktan sonra otomatik olarak:
1. `git add .` ile tüm değişiklikleri stage'le
2. Commit at
3. `git push origin main` ile push'la
(GitHub Pages workflow otomatik deploy edecek)
